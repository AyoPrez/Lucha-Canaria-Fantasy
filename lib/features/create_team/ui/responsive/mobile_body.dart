import 'package:flutter/material.dart';

class CreateTeamMobileBody extends StatelessWidget {
  const CreateTeamMobileBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blue,
    );
  }
}
