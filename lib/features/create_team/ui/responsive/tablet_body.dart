import 'package:flutter/material.dart';

class CreateTeamTabletBody extends StatelessWidget {
  const CreateTeamTabletBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.purple,
    );
  }
}