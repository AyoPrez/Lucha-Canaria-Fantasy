import 'package:injectable/injectable.dart';

@module
abstract class AppModule {
//   @preResolve
//   // Future<Parse> get parse = Parse().initialize(keyApplicationId, keyParseServerUrl,
//   // clientKey: keyClientKey, debug: true);
}
